#ifndef UTILS_H
#define UTILS_H

typedef enum
{
	LEFT,
	RIGHT,
	CENTER // i.e. neither Side
} side;

typedef enum
{
	FORWARD,
	BACKWARD,
	MIDDLE
} direction;

typedef enum
{
	VERTICAL,
	HORIZONTAL
} orientation;

#endif